source "https://rubygems.org"

# Local preview only — GitHub Pages builds the site server-side.
gem "jekyll", "~> 4.3"

group :jekyll_plugins do
  gem "jekyll-remote-theme"
  gem "jekyll-seo-tag"
end

# Removed from Ruby stdlib in 3.4+ / 4.x; Jekyll's deps still require them.
gem "base64"
gem "csv"
gem "logger"
gem "bigdecimal"
